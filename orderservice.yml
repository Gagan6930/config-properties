spring:
  datasource:
    url: jdbc:mysql://localhost:3306/RestApiOrder
    username: root
    password: Suma
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  boot:
    admin:
      client:
        url: http://localhost:1111
server:
  port: 8083
